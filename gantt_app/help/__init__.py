"""
Reference material shown from the application's Help buttons.

WHY THIS PACKAGE EXISTS:
=======================
Explanatory text has a habit of being pasted into whichever widget needs it,
where it competes for room with the controls and drifts out of step with the
behaviour it describes. Keeping it here gives each topic one home, lets a
window present it properly - scrollable, selectable, as long as it needs to
be - and leaves the dialogs holding nothing but their controls.
"""
